<%= div_for comment do %>
<h6>
 <li/> <%= comment.name %> - <%= comment.email %>:
  <% if @poem.owned_by? current_user %>
  <span class='actions'>

    <!-- póki co usuwanie schowamy...
			<%= link_to 'Delete', [@poem, comment], :confirm => 'Are you sure?', :method => :delete %>
		-->

  </span>
  <% end %>
</h1>
<p><%= comment.body %></p>

<% end %>
